fn main() {
    println!("Hello, world!");

    // Because x belongs to non-primitive data type which stores in 
    // Heap and in Rust we cannot create a shallow copy of non-primitive 
    // data type.
}
